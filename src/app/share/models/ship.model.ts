export interface ship {
  ship_id: number;
  shipName: string;
  imoNumber: number;
  absIdentification: number;
  portOfRegistry: string;
  grossTons: number;
  deadweight: number;
  dateOfBuild: Date;
}
