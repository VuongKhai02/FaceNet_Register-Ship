import { CertificateLoadPipe } from './certificate-load.pipe';

describe('CertificateLoadPipe', () => {
  it('create an instance', () => {
    const pipe = new CertificateLoadPipe();
    expect(pipe).toBeTruthy();
  });
});
