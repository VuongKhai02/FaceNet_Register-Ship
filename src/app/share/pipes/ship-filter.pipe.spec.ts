import { ShipFilterPipe } from './ship-filter.pipe';

describe('ShipFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
